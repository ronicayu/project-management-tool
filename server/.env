# PostgreSQL connection string
# Use Docker Postgres (npm run db:up) — port 5435 to avoid conflict with local Postgres on 5432
DATABASE_URL=postgresql://teu:teu@localhost:5435/teu

# Optional; default 3001
PORT=3001
